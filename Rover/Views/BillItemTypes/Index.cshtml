@model IEnumerable<Rover.Models.BillItemType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BillItemDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BillItemDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BillItemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BillItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BillItemID })
        </td>
    </tr>
}

</table>
