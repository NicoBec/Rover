@model IEnumerable<Rover.Models.BillMonth>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BillMonthDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BillMonthDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActiveBillMonth)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BillMonthDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BillMonthDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActiveBillMonth)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BillMonthID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BillMonthID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BillMonthID })
        </td>
    </tr>
}

</table>
