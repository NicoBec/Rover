@model IEnumerable<Rover.Models.BillItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BillItemAmmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BillableItem.BillableItemID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bill.BillID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BillItemAmmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BillableItem.BillableItemID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bill.BillID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BillItemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BillItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BillItemID })
        </td>
    </tr>
}

</table>
